/**
 * Integration Test Configuration - Delegates to Optimized Config
 * This maintains existing script compatibility while using the new architecture
 */
import integrationConfig from './config/vitest.integration.config.js';

/**
 * Export the optimized integration test configuration
 */
export default integrationConfig;