# DEPRECATED: This functionality has been integrated into post-merge-validation.yml
#
# The new workflow includes deployment validation as a single job
# instead of a separate workflow, reducing complexity and duplication.
#
# This file is kept for reference only and will be removed in a future update.
# DO NOT USE THIS WORKFLOW

name: "[DEPRECATED] Vercel Deployment Validation"

on:
  workflow_dispatch:  # Manual trigger only

jobs:
  deprecated-notice:
    runs-on: ubuntu-latest
    steps:
      - name: Deprecation Notice
        run: |
          echo "❌ This workflow is deprecated!"
          echo "Deployment validation is now part of 'Post-Merge Validation' workflow."
          echo "Location: .github/workflows/post-merge-validation.yml"
          exit 1
