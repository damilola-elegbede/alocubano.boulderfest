---
name: 🚀 Vercel Deployment Validation

# Validates Vercel deployments after they complete
# Ensures deployments are healthy and properly configured

on:
  deployment_status:
  workflow_dispatch:
    inputs:
      deployment_url:
        description: 'Deployment URL to validate'
        required: true
        type: string
  workflow_call:
    inputs:
      deployment_url:
        description: 'Deployment URL to validate'
        required: true
        type: string

env:
  NODE_VERSION: "20"
  CI: true

jobs:
  validate-deployment:
    name: 🔍 Validate Deployment
    runs-on: ubuntu-latest
    timeout-minutes: 10
    if: |
      github.event_name == 'workflow_dispatch' ||
      github.event_name == 'workflow_call' ||
      (github.event.deployment_status.state == 'success' && 
       github.event.deployment_status.environment_url != '')

    steps:
      - name: 📥 Checkout Code
        uses: actions/checkout@v4

      - name: 🔧 Setup Node.js ${{ env.NODE_VERSION }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: 📦 Install Dependencies
        run: npm ci --prefer-offline --no-audit

      - name: 🌐 Determine Deployment URL
        id: url
        run: |
          if [ -n "${{ inputs.deployment_url }}" ]; then
            DEPLOYMENT_URL="${{ inputs.deployment_url }}"
          elif [ -n "${{ github.event.deployment_status.environment_url }}" ]; then
            DEPLOYMENT_URL="${{ github.event.deployment_status.environment_url }}"
          else
            echo "❌ No deployment URL found"
            exit 1
          fi
          
          echo "deployment_url=$DEPLOYMENT_URL" >> $GITHUB_OUTPUT
          echo "🌐 Deployment URL: $DEPLOYMENT_URL"

      - name: 🏥 Health Check Validation
        run: |
          DEPLOYMENT_URL="${{ steps.url.outputs.deployment_url }}"
          HEALTH_URL="${DEPLOYMENT_URL}/api/health/check"
          
          echo "🏥 Validating deployment health..."
          echo "Health URL: $HEALTH_URL"
          
          # Wait for deployment to be fully ready
          for i in {1..12}; do
            echo "Health check attempt $i/12..."
            
            if curl -f -s --connect-timeout 30 --max-time 60 "$HEALTH_URL" > /dev/null; then
              echo "✅ Health check passed"
              
              # Get detailed health information
              HEALTH_RESPONSE=$(curl -s "$HEALTH_URL" || echo "Could not fetch health details")
              echo "Health response: $HEALTH_RESPONSE"
              break
            fi
            
            if [ $i -eq 12 ]; then
              echo "❌ Health check failed after 2 minutes"
              echo "Final attempt with verbose output:"
              curl -v --connect-timeout 30 --max-time 60 "$HEALTH_URL" || true
              exit 1
            fi
            
            echo "Waiting 10 seconds before retry..."
            sleep 10
          done

      - name: 🗃️ Database Health Validation
        run: |
          DEPLOYMENT_URL="${{ steps.url.outputs.deployment_url }}"
          DB_HEALTH_URL="${DEPLOYMENT_URL}/api/health/database"
          
          echo "🗃️ Validating database health..."
          echo "Database health URL: $DB_HEALTH_URL"
          
          if curl -f -s --connect-timeout 30 --max-time 60 "$DB_HEALTH_URL" > /dev/null; then
            echo "✅ Database health check passed"
            
            # Get detailed database health information
            DB_HEALTH_RESPONSE=$(curl -s "$DB_HEALTH_URL" || echo "Could not fetch database health details")
            echo "Database health response: $DB_HEALTH_RESPONSE"
          else
            echo "⚠️ Database health check failed (may be expected for some deployments)"
            echo "Attempting to get error details:"
            curl -v --connect-timeout 30 --max-time 60 "$DB_HEALTH_URL" || true
          fi

      - name: 🔗 API Endpoint Validation
        run: |
          DEPLOYMENT_URL="${{ steps.url.outputs.deployment_url }}"
          
          echo "🔗 Validating critical API endpoints..."
          
          # Test critical endpoints
          ENDPOINTS=(
            "/api/health/check"
            "/api/gallery"
            "/api/featured-photos"
          )
          
          SUCCESS_COUNT=0
          TOTAL_ENDPOINTS=${#ENDPOINTS[@]}
          
          for endpoint in "${ENDPOINTS[@]}"; do
            FULL_URL="${DEPLOYMENT_URL}${endpoint}"
            echo "Testing: $FULL_URL"
            
            if curl -f -s --connect-timeout 30 --max-time 60 "$FULL_URL" > /dev/null; then
              echo "✅ $endpoint - OK"
              ((SUCCESS_COUNT++))
            else
              echo "❌ $endpoint - Failed"
            fi
          done
          
          echo "API Endpoint Results: $SUCCESS_COUNT/$TOTAL_ENDPOINTS endpoints passed"
          
          # Require at least health check to pass
          if [ $SUCCESS_COUNT -eq 0 ]; then
            echo "❌ No API endpoints are responding"
            exit 1
          fi
          
          echo "✅ API endpoint validation completed"

      - name: 🌐 Static Asset Validation
        run: |
          DEPLOYMENT_URL="${{ steps.url.outputs.deployment_url }}"
          
          echo "🌐 Validating static asset delivery..."
          
          # Test critical static assets
          ASSETS=(
            "/favicon.ico"
            "/images/alocubano-logo.svg"
            "/css/main.css"
            "/js/main.js"
          )
          
          SUCCESS_COUNT=0
          TOTAL_ASSETS=${#ASSETS[@]}
          
          for asset in "${ASSETS[@]}"; do
            FULL_URL="${DEPLOYMENT_URL}${asset}"
            echo "Testing: $FULL_URL"
            
            if curl -f -s --connect-timeout 30 --max-time 60 "$FULL_URL" > /dev/null; then
              echo "✅ $asset - OK"
              ((SUCCESS_COUNT++))
            else
              echo "⚠️ $asset - Failed (may not exist)"
            fi
          done
          
          echo "Static Asset Results: $SUCCESS_COUNT/$TOTAL_ASSETS assets found"
          echo "✅ Static asset validation completed"

      - name: 📊 Generate Validation Report
        run: |
          DEPLOYMENT_URL="${{ steps.url.outputs.deployment_url }}"
          
          echo "# 🚀 Deployment Validation Report" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Deployment URL**: $DEPLOYMENT_URL" >> $GITHUB_STEP_SUMMARY
          echo "**Validation Time**: $(date -u)" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
          echo "## Validation Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ **Health Check**: Passed" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ **API Endpoints**: Core endpoints validated" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ **Static Assets**: Asset delivery validated" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
          echo "**Overall Status**: ✅ Deployment validation successful" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Next Steps**: Deployment is ready for use" >> $GITHUB_STEP_SUMMARY

  # Performance smoke test
  performance-smoke-test:
    name: ⚡ Performance Smoke Test
    runs-on: ubuntu-latest
    needs: validate-deployment
    timeout-minutes: 5
    if: success()

    steps:
      - name: 📥 Checkout Code
        uses: actions/checkout@v4

      - name: ⚡ Quick Performance Check
        run: |
          DEPLOYMENT_URL="${{ needs.validate-deployment.outputs.deployment_url || github.event.deployment_status.environment_url }}"
          
          if [ -z "$DEPLOYMENT_URL" ]; then
            echo "⚠️ No deployment URL available for performance testing"
            exit 0
          fi
          
          echo "⚡ Running performance smoke test..."
          echo "Target: $DEPLOYMENT_URL"
          
          # Simple response time check
          start_time=$(date +%s%N)
          
          if curl -f -s --connect-timeout 30 --max-time 60 "${DEPLOYMENT_URL}/api/health/check" > /dev/null; then
            end_time=$(date +%s%N)
            response_time=$(( (end_time - start_time) / 1000000 )) # Convert to milliseconds
            
            echo "✅ Response time: ${response_time}ms"
            
            if [ $response_time -lt 2000 ]; then
              echo "✅ Performance: Good (under 2 seconds)"
            elif [ $response_time -lt 5000 ]; then
              echo "⚠️ Performance: Acceptable (2-5 seconds)"
            else
              echo "⚠️ Performance: Slow (over 5 seconds)"
            fi
          else
            echo "❌ Performance smoke test failed - endpoint not responding"
          fi