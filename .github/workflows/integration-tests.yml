# DEPRECATED: This workflow has been replaced by post-merge-validation.yml
# 
# The new workflow fixes:
# - Port conflicts from hardcoded port 3000
# - Process cleanup issues  
# - Test redundancy (running same tests multiple times)
# - Inconsistent environment configurations
#
# This file is kept for reference only and will be removed in a future update.
# DO NOT USE THIS WORKFLOW

name: "[DEPRECATED] Integration Tests"

on:
  workflow_dispatch:  # Manual trigger only to prevent automatic runs

jobs:
  deprecated-notice:
    runs-on: ubuntu-latest
    steps:
      - name: Deprecation Notice
        run: |
          echo "❌ This workflow is deprecated!"
          echo "Please use 'Post-Merge Validation' workflow instead."
          echo "Location: .github/workflows/post-merge-validation.yml"
          exit 1